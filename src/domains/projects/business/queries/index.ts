import { ClientListQueryHandler } from './client.all.handler';

/**
 * @const {Array} queries - the list of queries
 * @since 0.0.1
 * @author Mark Leung <leungas@gmail.com>
 */
export const queries = [ClientListQueryHandler];
